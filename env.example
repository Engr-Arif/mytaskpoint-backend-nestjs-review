
# Environment Configuration Example
# Copy this file to ".env" and update the values as needed.

# --- Application ---
NODE_ENV=development
PORT=3000

# --- Database ---
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL=postgresql://postgres:password@localhost:5432/dtm_dev

# --- Authentication ---
# Generate secure random keys for JWT secrets before deploying
JWT_ACCESS_SECRET=replace-with-generated-access-secret
JWT_REFRESH_SECRET=replace-with-generated-refresh-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# --- Redis ---
# Used for caching and session management
# Example: redis://default:password@localhost:6379
REDIS_URL=redis://default:password@localhost:6379

# --- External Services ---
# Replace these with valid API keys when integrating external services
GOOGLE_MAPS_API_KEY=your-google-maps-api-key
SMS_API_KEY=your-sms-service-api-key
SMS_SENDER_ID=DTM
BRAND_NAME=DTM

# --- CORS ---
# Allowed origins (comma-separated). Use explicit domains in production.
ALLOWED_ORIGINS=http://localhost:3000

# --- Notes ---
# - Use different secrets and database credentials for each environment.
# - Rotate API keys and JWT secrets periodically.
# - Ensure environment variables are properly configured before deployment.

# Optional: Additional runtime flags
# LOG_LEVEL=info
